# pixi workspace — seeded by `shipit install` (the managed blocks below
# need a valid manifest). Consumer-owned from here on: edit freely.
[workspace]
name = "lex"
channels = ["conda-forge"]
platforms = ["linux-64", "linux-aarch64", "osx-64", "osx-arm64"]

[tasks]
# >>> shipit-managed test task (do not edit; regenerate via `shipit install`) >>>
test = "./bin/shipit test"
# <<< shipit-managed test task <<<
# >>> shipit-managed tasks (do not edit; regenerate via `shipit install`) >>>
changelog = "./bin/shipit changelog"
logs = "./bin/shipit logs"
# <<< shipit-managed tasks <<<

# Consumer-owned tasks (outside the shipit-managed block above).
# NOTE: `test` used to be declared HERE as a consumer-owned task
# (`cargo nextest run --workspace`), hand-rolled so `pixi run test` would not
# fall through to the POSIX `test` builtin (the shipit#444 trap, now CLOSED).
# The managed `pixi.toml#shipit-test-task` block above carries it: `test =
# "./bin/shipit test"`, which walks `.shipit.toml [toolchains]` (`"." = "rust"`)
# and dispatches the rust leg's `cargo nextest run` — the same runner over the
# same set (Cargo.toml here is a virtual workspace manifest, so a bare
# `cargo nextest run` at the root is `--workspace`). While this local task
# existed, that block hit a pixi key conflict and shipit SKIPPED delivering it
# with only a warning — so this repo was silently off the fleet task.

# The wf-checks TEST LANE's run (#828): the suite's fixtures live in the `comms`
# submodule, and the wf-checks block's checkout has no submodule support (the
# legacy rust-ci.yml caller passed `submodules: recursive`) — so the lane task
# provisions them inline, the same self-provisioning pattern as `lint-full`
# below. Idempotent and instant on a laptop where comms is already checked out.
test-full = "git submodule update --init --recursive && cargo nextest run --workspace"

[feature.lint.dependencies]
# >>> shipit-managed rust lint toolchain (do not edit; regenerate via `shipit install`) >>>
# The fleet-pinned rust lint toolchain (#547 Layer 1) — delivered because this
# repo tracks a Cargo.toml, which is exactly what makes the rust leg of `shipit
# lint` run (cargo clippy / cargo fmt per tracked manifest). conda-forge's
# `rust` is the official toolchain (cargo + clippy + rustfmt included), so the
# leg stops hard-failing (127) on hosts and CI runners without cargo (#526).
# Same pin as shipit's own [feature.test.dependencies] rust. If this repo no
# longer ships rust (the last Cargo.toml is gone), delete this block and its
# `pixi.toml#shipit-rust-lint-toolchain` entry in .shipit.toml's [managed]
# table — a reconcile only ADDS toolchain blocks, it never retires one.
rust = "1.96.*"
# <<< shipit-managed rust lint toolchain <<<
# >>> shipit-managed lint deps (do not edit; regenerate via `shipit install`) >>>
# The fleet-pinned lint toolchain (https://github.com/arthur-debert/shipit/blob/main/docs/legacy-prd/adoption.md:
# the managed set owns the consumer environment).
# Canonical versions live HERE — a bump is one data edit, rolled fleet-wide on
# each consumer's next `shipit install` reconcile.
ruff = "0.15.*"
# shellcheck is PINNED DOWN to 0.10.* (not 0.11): shellcheck 0.11.0 has an SC2218
# reaching-definitions false positive — a call to an EARLIER-defined function is
# flagged "defined later" when a subshell (`$(...)`, a pipeline component, `<(...)`)
# sits near the call, reddening the hermetic gate on CORRECT code across 5+ fleet
# repos. 0.11.0 is the latest upstream (no fixed 0.11.x yet); restore 0.11+ when
# upstream fixes it.
shellcheck = "0.10.*"
# conda-forge name for the `shfmt` binary
go-shfmt = "3.*"
yamllint = "1.38.*"
prettier = "3.8.*"
markdownlint-cli = "0.49.*"
# GitHub Actions workflow gate — the actions Lang of `shipit lint` (TOL01-WS04)
actionlint = "1.7.*"
lefthook = "2.1.*"
# <<< shipit-managed lint deps <<<

[feature.lint.tasks]
# >>> shipit-managed lint task (do not edit; regenerate via `shipit install`) >>>
lint = "./bin/shipit lint"
# <<< shipit-managed lint task <<<
# The wf-checks LINT LANE's run (#828) — the name `.shipit.toml [lanes.lint]`
# dispatches, kept as the lane-level sibling of `test-full`. The lint toolchain
# (ruff/shfmt/prettier/markdownlint/actionlint/rust 1.96) and lexd both live in
# THIS feature's env, so the lane task must resolve into the lint env; defined
# ONLY here, it exists in EXACTLY ONE env, and the wf-checks matrix job's
# `pixi run --locked lint-full` is identical to a laptop run.
#
# It is now the same command in the same env as the managed `lint` task above:
# since shipit#1066 that task lives in `[feature.lint.tasks]` too (it used to
# sit in the default `[tasks]`, where no lint tool exists on a hosted runner —
# the original reason for a separate lane task), and lexd rides the managed
# `shipit-lexd` feature block rather than an inline `provision lexd` (retired,
# ADR-0066). Unlike `test-full`, which still does real extra work (submodule
# init), this one is now a pure alias.
lint-full = { cmd = "./bin/shipit lint" }

[environments]
# >>> shipit-managed environments (do not edit; regenerate via `shipit install`) >>>
lint = ["lint", "shipit-lexd"]
# <<< shipit-managed environments <<<

# Consumer-owned dependencies (outside the shipit-managed blocks above).
# cargo-nextest provisions the `test` task's runner FROM pixi, so `pixi run test`
# works in a clean pixi env — not only where nextest already happens to be on
# PATH. The rest of the stack (cargo/rustc + the wasm32 std) is NO LONGER pinned
# here: the wf-checks test lane still runs `pixi run --locked test-full` on a
# hosted runner where nothing outside pixi is provisioned (ADP02-WS02, #828 —
# hosted images no longer carry Rust, and the pinned planner emits no env/cache
# descriptors that would put a toolchain on PATH), but the managed
# `pixi.toml#shipit-rust-release-toolchain` block below carries it in this same
# DEFAULT env, on the same conda-forge pin as [feature.lint.dependencies] rust.
[dependencies]
# >>> shipit-managed rust release toolchain (do not edit; regenerate via `shipit install`) >>>
# The rust RELEASE toolchain (#801, closing open hole 1 of the TOL02-WS17
# inventory): hosted runner images no longer carry Rust, and the managed rust
# LINT block anchors in the lint feature — an env the release runs' bare
# `pixi run --locked ./bin/shipit` never resolves. So `cargo` itself (prepare's
# subcommand dispatch, `cargo build`, `cargo publish`/`cargo metadata`)
# anchors HERE, in the DEFAULT env's [dependencies], exactly the PATH every
# wf-release stage executes with. conda-forge's `rust` is the official
# toolchain (cargo included), provisioned under setup-pixi's lockfile-keyed
# cache (the #582 doctrine: the release run NEVER installs at run time).
# Deliberately the SYSROOT-ONLY block, SEPARATE from the cargo-edit
# release-deps block: a consumer already pinning `rust` in its own
# [dependencies] (the pre-#801 consumer-side workaround) keeps its pin via
# the PixiKeyConflict first-splice guard WITHOUT losing cargo-edit delivery
# — a block shared with the release deps would be skipped whole. Pinned in
# lockstep with the rust lint block (drift-tested); a bump is one data edit
# per block, same diff. If this repo no longer ships rust (the last
# Cargo.toml is gone), delete this block and its
# `pixi.toml#shipit-rust-release-toolchain` entry in .shipit.toml's
# [managed] table — a reconcile only ADDS toolchain blocks, never retires one.
rust = "1.96.*"
# The wasm32 target std for this managed sysroot (#853): conda-forge's
# wasm-pack does NOT pull it — its only deps are __glibc/libgcc (osx: __osx),
# so the WS12 record's "wasm-pack pulls the wasm32 std" claim was false and
# every wasm build against the managed `rust` toolchain died with
# "wasm32-unknown-unknown target not found in sysroot" (found live in the
# ADP02 rust reconcile wave). Anchors HERE, beside the `rust` line it is a
# component of — NOT in the release-deps block — so a consumer that owns its
# `rust` pin skips the std WITH the toolchain (one sysroot, one owner: that
# consumer self-provisions the matching std, the sanctioned #759 rule)
# instead of receiving a std that misses its own rust version. Still no
# wasm-gated block (the WS12 record); accepted residue: ~20MB in every rust
# consumer's default env, wasm or not. Pinned in LOCKSTEP with the `rust`
# line above (drift-tested, `test_pins_agree_with_their_one_authority`) —
# bump both keys in the same diff, or the std misses the sysroot version.
rust-std-wasm32-unknown-unknown = "1.96.*"
# <<< shipit-managed rust release toolchain <<<
# >>> shipit-managed launcher deps (do not edit; regenerate via `shipit install`) >>>
# The pinned `bin/shipit` launcher's one prerequisite (#758, TOL02-WS17
# #794): every workflow block (wf-checks and the whole wf-release family)
# runs `pixi run --locked ./bin/shipit ...`, and the ADR-0033 launcher
# resolves the repo's shipit pin via `uv tool run` — but hosted runners do
# not carry uv, so without this block the very first `./bin/shipit` dies 127
# before any verb runs. Provisioned from conda-forge under setup-pixi's
# lockfile-keyed cache (the #582 doctrine; the spike measured the launcher's
# uv resolve at 5s cold / 0-3s warm riding this env — no extra cache step).
# Anchored under [dependencies] — the DEFAULT env — because that is exactly
# the PATH a bare `pixi run --locked <task>` resolves. Pinned to the minor
# line of Layer 0's UV_PIN (bin/setup-dev-env.sh), so dev machines and CI
# provision the same uv; a lockstep drift test (tests/test_install.py) fails
# when the two move apart. `uv build` (the python bundle composition) rides
# this same entry.
uv = "0.11.*"
# <<< shipit-managed launcher deps <<<
# >>> shipit-managed conda packager (do not edit; regenerate via `shipit install`) >>>
# The conda ENDPOINT's packager (ARF01-WS01 #950, ADR-0064/0070; re-gated
# #1071). `shipit release publish`'s conda endpoint runs `rattler-build build`
# to repackage a final release binary into a `.conda` and `rattler-build
# publish` to push+reindex the producing repo's per-repo Artifact channel.
# rattler-build is the ENDPOINT's tool, not a toolchain tool — so this block is
# delivered iff the repo declares a `conda` endpoint on ANY `[artifacts.*]`
# (any composition — a rust binary, a tree-sitter `tarball` grammar, a future
# python/lua producer), toolchain-INDEPENDENT. Its former home was the rust
# release-deps block (rust repos were the first conda producers, WS03/ARF02),
# which starved a NON-rust conda producer of its packager — the #1071 seed
# failure (tree-sitter-lex declared conda but `publish` died `No such file …
# rattler-build`). rattler-build is on conda-forge, so it anchors in the
# DEFAULT env's [dependencies] — the wf-release stages execute shipit via bare
# `pixi run --locked ./bin/shipit`, so the packager must be on THAT run's PATH.
# Provisioned under setup-pixi's lockfile-keyed cache (the #582 doctrine: the
# release run NEVER installs at run time — publish fails loudly naming this
# reconcile instead). Pinned to the minor line (`0.69.*` — conda match-spec);
# seed-validated at 0.69 against the live channel (#1049 — 0.68.* panicked
# during the S3 upload: `opendal-core 0.57.0 … concurrent tasks executed with
# no executor`, even with correct AWS_* creds). A bump is one data edit, rolled
# out on each consumer's next `shipit install` reconcile.
rattler-build = "0.69.*"
# <<< shipit-managed conda packager <<<
# >>> shipit-managed node deps (do not edit; regenerate via `shipit install`) >>>
# The fleet-pinned node runtime (#547 Layer 1) — delivered because this repo
# tracks a package.json. Unlike the rust/go blocks this is NOT a linter
# toolchain: it provisions the repo's OWN node/pnpm runtime (npm rides the
# nodejs package), which is why it lives in the DEFAULT env's [dependencies]
# rather than the lint feature. If this repo no longer ships node (the last
# package.json is gone), delete this block and its `pixi.toml#shipit-node-deps`
# entry in .shipit.toml's [managed] table — a reconcile only ADDS toolchain
# blocks, it never retires one.
nodejs = "26.*"
pnpm = "11.*"
# <<< shipit-managed node deps <<<
# >>> shipit-managed rust release deps (do not edit; regenerate via `shipit install`) >>>
# The rust RELEASE-side bump tool (#793, the #784-F2 failure class): `shipit
# release prepare`'s rust bump adapter runs `cargo set-version --workspace`
# (a cargo-edit subcommand), and wf-prepare executes shipit in the DEFAULT
# pixi env (`pixi run --locked ./bin/shipit`), so cargo-edit anchors HERE —
# [dependencies], not a feature — where that run's PATH resolves it (cargo
# finds subcommands via $CARGO_HOME/bin then PATH, the #785 finding).
# Provisioned from conda-forge under setup-pixi's lockfile-keyed cache (the
# #582 doctrine: the release run NEVER `cargo install`s). Pinned to the patch
# line (`0.13.11.*` — conda match-spec, so any 0.13.11.x build resolves); a
# bump is one data edit, rolled out on each consumer's next `shipit install`
# reconcile. If this repo no longer ships rust (the last Cargo.toml is gone),
# delete this block and its `pixi.toml#shipit-rust-release-deps` entry in
# .shipit.toml's [managed] table — a reconcile only ADDS toolchain blocks, it
# never retires one.
cargo-edit = "0.13.11.*"
# The wasm/npm bundle composition's builder (TOL02-WS12 #788, WS10 DECIDED
# #798): `shipit release bundle`'s wasm-pack composition runs `wasm-pack build`
# to turn a rust crate into an npm package tree, then `npm pack`s the tarball
# — the artifact. wasm-pack is on conda-forge, so it anchors HERE (the rust
# release-deps block, rust signal) beside cargo-edit rather than
# self-provisioning (the cargo-deb #785 exception is for tools conda-forge does
# NOT carry). Provisioned under setup-pixi's lockfile-keyed cache (the #582
# doctrine: the release run NEVER installs at run time). Pinned to the
# minor line (`0.15.*` — conda match-spec); a bump is one data edit, rolled out
# on each consumer's next `shipit install` reconcile. 0.15.* per #846: the
# original 0.13.* pin was never solvable — conda-forge never published any
# 0.13.x wasm-pack build (verified on osx-arm64 + linux-64) — so every
# consumer's default env died at solve.
wasm-pack = "0.15.*"
# NOTE: wasm-pack does NOT pull the wasm32 target std — its only conda deps
# are __glibc/libgcc (osx: __osx), so the WS12 record's claim that it did was
# false (#853). The std is a sysroot COMPONENT, so it anchors in the
# rust-release-toolchain block beside `rust`, NOT here: it must be delivered
# (and skipped, when a consumer owns its `rust` pin) with the sysroot it
# belongs to, while this block delivers unconditionally on the rust signal.
# NOTE: rattler-build (the conda endpoint's packager) used to ride HERE too
# (ARF01-WS01 #950) — but it is the ENDPOINT's tool, not a rust tool, and
# gating it on the rust signal starved a NON-rust conda producer (tree-sitter's
# `tarball` grammar) of its packager (the #1071 seed failure). It now rides the
# conda-endpoint-gated `pixi.toml#shipit-conda-packager` block
# (src/shipit/data/pixi-conda-packager-block.toml), delivered iff the repo
# declares a `conda` endpoint — so a rust+conda repo gets it EXACTLY once and a
# rust-without-conda repo (which publishes no `.conda`) no longer carries it.
# <<< shipit-managed rust release deps <<<
cargo-nextest = ">=0.9.138,<0.10"
# NOTE: `rust` and `rust-std-wasm32-unknown-unknown` used to be pinned HERE as
# consumer-owned entries — the same story as `uv` below. The managed
# `pixi.toml#shipit-rust-release-toolchain` block now carries both: `rust =
# "1.96.*"` (the byte-identical pin this repo was shadowing) AND the
# wasm32-unknown-unknown sysroot std beside it (shipit#854), which the lex-wasm
# artifact's wasm-pack composition needs or it dies at "wasm32-unknown-unknown
# target not found in sysroot" (lex#832). The hand-declared std existed ONLY
# because the local `rust` pin made shipit skip that very block, so it was
# self-cancelling and goes with it. While these local pins existed, the block
# hit a pixi key conflict and shipit SKIPPED delivering it with only a warning
# — so this repo was silently off the fleet pin.
# NOTE: `uv` (the pinned bin/shipit launcher's one prerequisite, ADR-0033) used
# to be pinned HERE as a consumer-owned entry, flagged at the time as something
# the managed set should own (lex#828/#829). It now does: the managed
# `pixi.toml#shipit-launcher-deps` block below carries it. While this local pin
# existed, that block hit a pixi key conflict and shipit SKIPPED delivering it
# with only a warning — so this repo was silently off the fleet pin.

# >>> shipit-managed lexd feature (do not edit; regenerate via `shipit install`) >>>
[feature.shipit-lexd]
# lexd — the lex Lang's lint-gate tool — resolved as an ordinary conda package
# from the public Artifact channel (ADR-0066: `provision lexd` retired). The
# channel is scoped to THIS reserved feature (a sibling of conda-forge, added to
# whatever env `shipit-lexd` is wired into — the lint env), so lexd rides
# pixi.lock and pixi's sha256 like every other linter, and `shipit install`
# keeps the whole fleet on ONE lexd version — uniformity moved from a compiled
# binary constant to this managed block (ADR-0047: not consumer config).
channels = ["https://storage.googleapis.com/shipit-artifacts-public/lex-fmt/lex"]

# The fleet-pinned lexd, PLATFORM-SCOPED via pixi `[target]` tables. This file is
# the block HEADER; `shipit install` GENERATES one
# `[feature.shipit-lexd.target.<plat>.dependencies]` table below per platform in
# (this repo's declared `[workspace].platforms` ∩ the channel's CLOSED SERVED SET
# — `SERVED_SUBDIRS`, `src/shipit/channel/buckets.py`:
# osx-arm64/linux-64/linux-aarch64 AND win-64), pinned to `LEXD_PIN`
# (`src/shipit/install/units.py`, the single pin edit) — NEVER a blanket
# `[feature.shipit-lexd.dependencies]`. See `units.lexd_block`; a bump is one edit
# to `LEXD_PIN`, rolled fleet-wide on each consumer's next reconcile.
#
# Why intersect with the repo's OWN declared platforms (#1072): a `[target]`
# selector for a platform the workspace does NOT declare makes pixi warn on EVERY
# invocation (`target selector 'win-64' does not match any of the platforms
# supported by the workspace`) — fleet-wide noise (hit on shipit's own `main`
# after #1068 emitted a fixed all-of-served target set). A repo emits a target
# only for a platform it actually declares.
#
# Why intersect with the served set (#1068) — load-bearing AND fail-closed:
#  - A BLANKET `[feature.shipit-lexd.dependencies]` applied lexd to EVERY platform
#    a composing env declares, so a consumer declaring a platform OUTSIDE the
#    served set (e.g. osx-64, Intel Mac) had no channel candidate and got an
#    unsatisfiable lint-env solve — `shipit install` then failed closed with no
#    commit and no reconcile (#1068). Platforms NOT in the served set carry NO
#    lexd dep, so their lint env SOLVES and install/reconcile no longer breaks.
#  - win-64 IS in the served set (served, merely owner-PAUSED under the Windows
#    build pause #895, ADR-0071). A repo that DECLARES win-64 keeps a win-64
#    target and FAILS CLOSED at solve (the channel serves no win-64 lexd while the
#    pause holds — ADR-0071's "Windows consumers fail closed, loudly"); a repo
#    that does NOT declare win-64 emits no win-64 target (and no dangling-selector
#    warning). It re-resolves automatically when #895 lifts — no edit here.
[feature.shipit-lexd.target.osx-arm64.dependencies]
lexd = "==0.19.10"
[feature.shipit-lexd.target.linux-64.dependencies]
lexd = "==0.19.10"
[feature.shipit-lexd.target.linux-aarch64.dependencies]
lexd = "==0.19.10"
# <<< shipit-managed lexd feature <<<
